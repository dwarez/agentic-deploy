# Core AWS dependencies for deployment work.
#
# No version pins. We want the latest — if scripts break against the latest,
# fix the script, not the version. Newer boto3/awscli have better error
# messages, more current API surface, and security fixes.
#
# Note: the SageMaker Python SDK is deliberately NOT listed. Our scripts use
# boto3 directly and construct image URIs manually. See python-env-setup
# SKILL.md ("Why no sagemaker package") and the PR that introduced this
# decision for context.
#
# Add per-deployment extras (e.g. huggingface_hub, transformers for local
# validation) to this file or to a deployment-specific requirements file
# alongside it. Do not pin versions here unless you have a documented
# reason and a plan to unpin later.

boto3
awscli
