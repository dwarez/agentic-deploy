hey, I need to deploy a model on AWS SageMaker and I don't really want to deal with all the console clicking and boto3 stuff myself. can you handle it?

the model is Qwen 3.5 0.8B (take it from hf hub). it's going to be called from an internal app, not super high traffic, but I want it to respond reasonably fast.

figure out the best way to deploy it and just walk me through it. ask me whatever you need to know before you start doing things — I'd rather answer a few questions upfront than have you spin up something expensive I didn't want.

two things I really need:
1. before you start, write out the plan as a file I can look at so I know what you're about to do and can tweak it
2. keep a log of every action you take — every aws call, every file you create, every command you run — append it to a log file as you go. I want to be able to look back later and see exactly what happened, in order, with timestamps. if something breaks I need to know where.

go.
